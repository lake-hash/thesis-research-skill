import test from 'node:test';import assert from 'node:assert/strict';
import {triageCandidateHash} from './candidate-accounting-contract.mjs';
import {validateFactCandidateClosure} from './fact-candidate-closure-contract.mjs';
const candidate={source_id:'source-a',authorship:'author_owned',disposition:'candidate',investable_object:'resolved',what_present:true,why_present:true,possible_increment:'changed_condition',content_domain:'mixed',ticker_hints:['XYZ']};
const triage={version:'triage/1.0',decisions:[candidate]},archive={sources:[{id:'source-a',text:'XYZ may fall until policy changes.'}]},facts={records:[],decisions:[{source_id:'source-a',disposition:'source_only',reason:'reviewed'}]};
test('complete candidates cannot disappear between Facts and Language',()=>{const result=validateFactCandidateClosure(triage,facts,archive);assert.equal(result.ok,false);assert.match(result.errors[0],/disappeared before Language/);});
test('exact evidence-bound hard exclusions close a complete candidate',()=>{const review={version:'triage-candidate-accounting/1.0',triage_sha256:triageCandidateHash(triage),reviewer:'source-first reviewer',reviewed_at:'2026-09-20T00:00:00Z',decisions:[{source_id:'source-a',decision:'exclude',basis:'investment_landing_missing',reason:'The expression states a condition without an investment landing.',evidence:[{quote:'XYZ may fall until policy changes.',explanation:'The full expression contains no buy, avoid, valuation or durable investment judgment.'}]}]};const result=validateFactCandidateClosure(triage,facts,archive,review);assert.equal(result.ok,true);});
